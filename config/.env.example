# Copy to config/.env and fill in. config/.env is gitignored.
#
# A Splunk account with admin capabilities on the dev instance: creating
# indexes and apps, streaming events, and later creating test users and roles.
SPLUNK_USERNAME=admin
SPLUNK_PASSWORD=
